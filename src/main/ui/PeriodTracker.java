package ui;

import model.PeriodEntry;

public class PeriodTracker {
    //fields

    /*
     * EFFECTS: creates an instance of the PeriodTracker application and runs the application by calling the run method.
     */
    public PeriodTracker() {
        //stub
    }

    /*
     * EFFECTS: runs the application.
     */
    private void run() {
        //stub
    }

    /*
     * EFFECTS: displays a menu to the user with possible functionalities.
     */
    private void menu() {

    }

    /*
     * REQUIRES: string must not be empty.
     * EFFECTS: processes the chosen option as selected by the user from the menu as long as it's not to quit the application.
     */
    private void processOption(String option) {
        //stub
    }

    /*
     * EFFECTS: creates a new period entry object by asking user for the date they are entering information for. 
     */
    private void logPeriod() {
        //stub
    }

    /*
     * EFFECTS: user can modify an entry by entering a date for the entry and then logging new information.
     */
    private void modifyEntry() {
        //stub
    }

    /*
     * 
     */
    private void analyze() {
        //stub
    }

    /*
     * EFFECTS: asks user if they want to view a particular entry or view all the entries and displays accordingly.
     */
    private void viewLog() {
        //stub
    }

    /*
     * EFFECTS: clears the log of entries.
     */
    private void clearLog() {
        //stub
    }

    /*
     * REQUIRES: entry must not be null.
     * EFFECTS: asks user if they would like to log particular aspects (heaviness, pain areas, etc.) for entry.
     */
    private void logParameters(PeriodEntry entry) {
        //stub
    }
}
